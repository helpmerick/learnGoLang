package main

import "fmt"

func main() {
	a := "noow"
	b := "neveer"
	if a == ("now") {
		fmt.Println("Now is the time!")
	} else if b == "never" {
		fmt.Println("Never again.")
	} else {
		fmt.Println("Check back later.")
	}
}
